# My aliases
alias ll = ls -al
alias la = ls -am
alias cat = bat -P

# Git
#
alias lg = lazygit

# Eza
alias ezt = eza --tree
alias ezta = eza --tree --all
alias ezl = eza --long
alias ezla = eza --long --all

# Bat
alias b = bat
alias bn = bat --number
alias bnl = bat --number --line-range
alias bp = bat --plain
alias bpl = bat --plain --line-range
alias bl = bat --line-range
